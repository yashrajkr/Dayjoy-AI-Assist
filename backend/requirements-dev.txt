# Backend test dependencies. Install with:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt
# Run with:
#   pytest backend/tests
-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
